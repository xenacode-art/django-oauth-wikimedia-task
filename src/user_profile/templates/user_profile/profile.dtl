<!DOCTYPE html>
<html>
<head>
    <title>Profile - My first Django OAuth app</title>
</head>
<body>
    <h1>Profile</h1>
    <p>You are logged in as: <strong>{{ user }}</strong></p>
    <p>Username: {{ user.username }}</p>
    <p>Email: {{ user.email }}</p>

    {% if has_oauth %}
    <h2>OAuth Status</h2>
    <p style="color: green;">✓ Connected with Wikimedia OAuth</p>
    <p><em>Your account is authenticated and can be used with Pywikibot for automated wiki operations.</em></p>
    {% else %}
    <p style="color: red;">✗ No OAuth credentials found</p>
    {% endif %}

    <p><a href="/">Back to Home</a></p>
</body>
</html>
