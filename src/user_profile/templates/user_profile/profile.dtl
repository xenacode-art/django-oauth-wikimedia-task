<!DOCTYPE html>
<html>
<head>
    <title>Profile - My first Django OAuth app</title>
</head>
<body>
    <h1>Profile</h1>
    <p>You are logged in as: <strong>{{ user }}</strong></p>
    <p>Username: {{ user.username }}</p>
    <p>Email: {{ user.email }}</p>

    {% if has_oauth %}
    <h2>OAuth Information</h2>
    <p>OAuth Token: <code>{{ oauth_token }}</code></p>
    <p style="color: green;">✓ Connected with Wikimedia OAuth</p>
    <p><em>Your OAuth credentials can be used with Pywikibot for automated wiki operations.</em></p>
    {% else %}
    <p style="color: red;">✗ No OAuth credentials found</p>
    {% endif %}

    <p><a href="/">Back to Home</a></p>
</body>
</html>
